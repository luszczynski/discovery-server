---

- name: internal_host_started_processing_role
  set_fact:
    internal_host_started_processing_role: "jvm"

# - name: gather internal_xms_size for Process Controller
#   raw: ps -A -o comm -o args e --columns 10000 | grep java | grep jboss | grep -v "/usr/bin/grep" | grep "Process Controller" | grep -Pio '(?<=xms)[^ ]+' | uniq
#   register: internal_process_controller_xms_size
#   ignore_errors: yes
#   when: 'jboss_eap'

# - name: set xms_size fact for process controller
#   set_fact:
#     process_controller_xms_size: "{{ internal_process_controller_xms_size.stdout | trim }}"
#   ignore_errors: yes
#   when: 'jboss_eap'

- name: gather internal_xms_size
  raw: ps -A -o comm -o args e --columns 10000 | grep java | grep jboss | grep -v "/usr/bin/grep" | grep -Pio '(?<=xms)[^ ]+'
  register: internal_xms_size
  ignore_errors: yes
  when: 'jboss_eap'

- name: set xms_size fact
  set_fact:
    xms_size: "{{ internal_xms_size.stdout | trim }}"
  ignore_errors: yes
  when: 'jboss_eap'

- name: gather internal_xmx_size
  raw: ps -A -o comm -o args e --columns 10000 | grep java | grep jboss | grep -v "/usr/bin/grep" | grep -Pio '(?<=xmx)[^ ]+'
  register: internal_xmx_size
  ignore_errors: yes
  when: 'jboss_eap'

- name: set xmx_size fact
  set_fact:
    xmx_size: "{{ internal_xmx_size.stdout | trim }}"
  ignore_errors: yes
  when: 'jboss_eap'

- name: gather internal max perm size
  raw: ps -A -o comm -o args e --columns 10000 | grep java | grep jboss | grep -v "/usr/bin/grep" | grep -Pio '(?<=MaxPermSize=)[^ ]+'
  register: internal_max_perm_size
  ignore_errors: yes
  when: 'jboss_eap'

- name: set internal max perm size fact
  set_fact:
    max_perm_size: "{{ internal_max_perm_size.stdout | trim }}"
  ignore_errors: yes
  when: 'jboss_eap'

- name: gather internal_command_line_jboss
  raw: ps -A -o comm -o args e --columns 10000 | grep java | grep jboss | grep -v "/usr/bin/grep"
  register: internal_command_line_jboss
  ignore_errors: yes
  when: 'jboss_eap'

- name: set command_line_jboss fact
  set_fact:
    command_line_jboss: "{{ internal_command_line_jboss.stdout | trim }}"
  ignore_errors: yes
  when: 'jboss_eap'
